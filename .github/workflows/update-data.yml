Display name: David Furness
Username: david.furness@shelfordgroup.org
Password: Jay554488!?

Laptop login password
RedCup25!?


201221 Bitlocker
531943 Windows pinname: Update NHS Data

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Fetch NHS data
      run: |
        python fetch_data.py
        
    - name: Commit and push if data changed
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add data/
        git diff --quiet && git diff --staged --quiet || (git commit -m "Update NHS data - $(date +'%Y-%m-%d')" && git push)